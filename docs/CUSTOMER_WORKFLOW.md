# Customer Workflow

Store channel → product preview → purchase button → bot → order summary → payment method → provider checkout → verified callback → paid order → file delivery → delivery confirmation.

Customer must never receive main files before payment is verified.
